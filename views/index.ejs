<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quora Posts</title>
    <link rel="stylesheet" href="/style.css">
    </link>
</head>

<body>
    <h1>Quora Posts</h1>
    <% for(let post of posts){ %>
        <div class="post">
            <h3 class="username">@<%= post.username %>
            </h3>
            <h4 class="content">
                <%= post.content %>
            </h4>
            <a href="http://localhost:9080/posts/<%= post.id %>">Edit</a>
            <a href="http://localhost:9080/posts/<%= post.id %>/edit">Edit Check</a>
            <br>
            <form method="post" action="/posts/<%= post.id %>?_method=DELETE">
                <button>Delete</button>
            </form>
        </div>
        <% } %>
            <br>
            <a href="http://localhost:9080/posts/new">
                Submit a New Post
            </a>

</body>

</html>
<!-- for(let post of posts){
<div>
    <h3>@ post.username</h3>
    <h4>post.content</h4>
</div>
} -->